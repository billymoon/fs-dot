#!/usr/bin/env node

const path = require('path')

const fs = require('fs-extra')

const args = process.argv.slice(2)

if (args.length === 1) {
  fs.ensureDirSync(args[0])
} else {
  console.log(`Usage:

  fs.mkdirp directory/to/be/cleaned
`)
}
