#!/usr/bin/env node

const path = require('path')

const fs = require('fs-extra')

const args = process.argv.slice(2)

if (args.length === 1) {
  fs.emptyDirSync(args[0])
} else {
  console.log(`Usage:

  fs.clean directory/to/be/cleaned
`)
}
